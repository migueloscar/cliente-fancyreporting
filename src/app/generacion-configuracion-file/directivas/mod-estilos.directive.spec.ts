import { ModEstilosDirective } from './mod-estilos.directive';

describe('ModEstilosDirective', () => {
  it('should create an instance', () => {
    const directive = new ModEstilosDirective();
    expect(directive).toBeTruthy();
  });
});
